A collection of configuration files I use on NixOs, without any major changes to what you would see on a "default" configuration.
Make sure you have added the `unstable` branch to your `nixos-channels`, and have both flakes and home manager correctly installed on your system. \
(Upload of files pending)
